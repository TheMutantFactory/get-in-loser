# TODO

## Hosting / social preview
- [ ] **Fill in the public host URL for the social meta tags in `index.html`.**
  The absolute-URL social tags are commented out (hidden) because the hosting
  URL isn't decided yet, so they don't ship a guessed/broken address. Once the
  site has a real home (e.g. GitHub Pages `https://<user>.github.io/get-in-loser/`
  or a custom domain), replace `https://YOUR-HOST/` and uncomment:
  - `og:url`
  - `og:image`
  - `twitter:image`
  - `itemprop="image"`

  The preview image itself is ready: `images/preview.jpg` (branded, 1200x630).
